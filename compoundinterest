#include<stdio.h>
#include<math.h>
int main()
{
    double p=10000;
    double r=5;
    double t=3;
    double a=p*((pow((1+r/100),t)));
    double ci=a-p;
    printf("compound interest is:%lf",ci);
    return 0;
}
    


